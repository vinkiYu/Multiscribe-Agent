# 执行包：P21 — LLM-as-Judge 评估框架

> **阶段**：阶段三（与 P20.1 + P22 并行）
> **目标**：用 LLM 作为裁判，从三个维度评估 Pipeline 输出质量，并支持回归检测。
> **依赖**：P11 (digest pipeline)、P17 (memory preferred_tags)、现有 Reflector。
> **并行说明**：与 P20.1 / P22 同期可独立开发；P24 Loop 深化依赖本包。

---

## 一、目标与验收口径

**核心目标**：
- 提供可重放的「输入 → Pipeline → LLM-as-Judge 打分」评估链路。
- 三个维度并行打分：**摘要质量 / 推荐相关性 / 流程稳定性**。
- 每次跑完输出 **Markdown 报告**，分数下降 > 10% 时 **触发告警**。

**验收口径**：
- CLI：`multiscribe-agent eval --dataset tech-weekly --agent default-curation-agent` 可跑。
- 预置数据集 + 评分函数 + 报告生成均独立可测。
- 不修改 Pipeline 主流程，仅通过现有 `pipeline_state` JSON 读输入、读产出。

---

## 二、验收条件

| # | 验收 | 证据 |
|---|---|---|
| 1 | `multiscribe-agent eval --dataset tech-weekly` 可执行 | CLI 输出 OK |
| 2 | `src/multiscribe_agent/eval/dataset.py` 可加载 YAML 数据集 | 单测 `test_dataset.py` |
| 3 | 三维度评分返回：accuracy / conciseness / format / relevance / stability | 单测 `test_evaluator.py` |
| 4 | 回归检测：分数下降 > 10% 触发 `RegressionDetected` 异常 | 单测 `test_benchmark.py` |
| 5 | 报告输出到 `data/eval/reports/{dataset}_{timestamp}.md` | 实跑产出 |
| 6 | 不修改 Pipeline，仅读取 `pipeline_state` JSON | 黑名单强约束 |
| 7 | 全量 pytest + ruff + mypy 通过 | `pytest -q` / `ruff check .` / `mypy src` |

---

## 三、可改范围（白名单）

| 文件路径 | 操作 |
|---|---|
| `src/multiscribe_agent/eval/__init__.py` | **新增**（导出 public API） |
| `src/multiscribe_agent/eval/dataset.py` | **新增**（YAML 数据集加载 + schema 校验） |
| `src/multiscribe_agent/eval/evaluator.py` | **新增**（三维度评分函数 + LLM 调用封装） |
| `src/multiscribe_agent/eval/benchmark.py` | **新增**（批量跑 + 回归检测 + 报告生成） |
| `src/multiscribe_agent/eval/judge_prompts.py` | **新增**（三个 rubric 的 prompt 模板） |
| `data/eval/datasets/tech_weekly.yaml` | **新增**（预置数据集 ≥ 5 个样本） |
| `data/eval/datasets/summary_quality.yaml` | **新增**（预置数据集 ≥ 3 个样本） |
| `src/multiscribe_agent/cli.py` | **修改**（追加 `eval` 子命令） |
| `src/multiscribe_agent/eval/__main__.py` | **新增**（`python -m multiscribe_agent.eval ...`） |
| `tests/eval/__init__.py` | **新增** |
| `tests/eval/test_dataset.py` | **新增** |
| `tests/eval/test_evaluator.py` | **新增** |
| `tests/eval/test_benchmark.py` | **新增** |
| `tests/eval/fixtures/*.json` | **新增**（模拟 pipeline_state 样本） |

---

## 四、禁止改动（黑名单）

- `src/multiscribe_agent/pipelines/`（不得修改每日推送主流程）
- `src/multiscribe_agent/agents/reflector.py`（已有 Reflector；本包只读不写）
- `src/multiscribe_agent/agents/workflow/loop_node.py`（P24 才扩，本包仅观察）
- `src/multiscribe_agent/api/`（不暴露 eval HTTP 接口，仅 CLI）
- `frontend/`、`docs/`、`codex/`（除本任务包外的所有文档）

---

## 五、详细任务

### T1：dataset.py（YAML 数据集加载）

```python
# src/multiscribe_agent/eval/dataset.py
"""YAML-based evaluation datasets with strict schema validation."""

from __future__ import annotations

from dataclasses import dataclass
from pathlib import Path
from typing import Literal

import yaml
from pydantic import BaseModel, Field, ValidationError


class DatasetSample(BaseModel):
    id: str = Field(min_length=1)
    input_path: str  # pipeline_state.json path
    expected_tags: list[str] = Field(default_factory=list)
    expected_min_score: float = Field(default=0.7, ge=0.0, le=1.0)
    notes: str = ""


class Dataset(BaseModel):
    name: str = Field(min_length=1)
    description: str
    samples: list[DatasetSample] = Field(min_length=1)
    rubric: Literal["tech-weekly", "summary-quality"] = "tech-weekly"


def load_dataset(path: Path) -> Dataset:
    """Load and validate a YAML dataset file."""
    raw = yaml.safe_load(path.read_text(encoding="utf-8"))
    try:
        return Dataset.model_validate(raw)
    except ValidationError as exc:
        raise ValueError(f"Invalid dataset {path}: {exc}") from exc
```

### T2：judge_prompts.py（三 rubric 模板）

```python
# src/multiscribe_agent/eval/judge_prompts.py
"""Rubric prompt templates for LLM-as-Judge scoring."""

SUMMARY_RUBRIC = """You are evaluating a Chinese newsletter summary produced by an AI Agent.
Rate the following three dimensions on a 1-10 scale (integers only):

1. accuracy (事实准确性；是否与原文一致，无捏造)
2. conciseness (简洁性；是否去除冗余，信息密度高)
3. format (格式规范性；是否符合 Markdown 标题/列表/段落规范)

Return ONLY JSON: {"accuracy":N,"conciseness":N,"format":N,"overall":N}
where overall = round((accuracy + conciseness + format) / 3)."""

RELEVANCE_RUBRIC = """Evaluate recommendation relevance between user preferences and selected items.

User preferred_tags: {tags}
Selected items tags: {item_tags}

Return ONLY JSON: {"relevance":N,"matched":N,"total":N,"reason":"..."}
where relevance = matched / total * 10 (integer, 0-10)."""

STABILITY_RUBRIC = """Evaluate the stability of the pipeline run based on log evidence.

Stats: {stats}

Return ONLY JSON: {"stability":N,"bottleneck":"...","reason":"..."}
where stability = weighted score (RSS 30% + LLM 40% + Publish 30%), integer 0-10."""
```

### T3：evaluator.py（三维度评分函数）

```python
# src/multiscribe_agent/eval/evaluator.py
"""LLM-as-Judge evaluator with three scoring dimensions."""

from __future__ import annotations

import json
from dataclasses import dataclass
from pathlib import Path
from typing import Literal

from multiscribe_agent.domain.models import AIMessage
from multiscribe_agent.eval.judge_prompts import (
    RELEVANCE_RUBRIC,
    STABILITY_RUBRIC,
    SUMMARY_RUBRIC,
)
from multiscribe_agent.llm.provider import AIProvider


@dataclass(frozen=True, slots=True)
class SummaryScores:
    accuracy: int
    conciseness: int
    format: int
    overall: int


@dataclass(frozen=True, slots=True)
class RelevanceScores:
    relevance: int
    matched: int
    total: int
    reason: str


@dataclass(frozen=True, slots=True)
class StabilityScores:
    stability: int
    bottleneck: str
    reason: str


@dataclass(frozen=True, slots=True)
class EvaluationResult:
    sample_id: str
    summary: SummaryScores
    relevance: RelevanceScores
    stability: StabilityScores
    passed: bool


class JudgeError(ValueError):
    """Raised when the LLM-as-Judge response cannot be parsed."""


async def _ask_judge(provider: AIProvider, prompt: str, system: str) -> dict[str, object]:
    response = await provider.generate(
        [AIMessage(role="user", content=prompt)],
        system_instruction=system,
    )
    try:
        payload = json.loads(response.content)
    except json.JSONDecodeError as exc:
        raise JudgeError(f"Judge returned non-JSON: {response.content!r}") from exc
    if not isinstance(payload, dict):
        raise JudgeError("Judge payload must be a JSON object")
    return payload


async def score_summary(provider: AIProvider, summary: str) -> SummaryScores:
    payload = await _ask_judge(provider, f"Summary to evaluate:\n{summary}", SUMMARY_RUBRIC)
    return SummaryScores(
        accuracy=int(payload["accuracy"]),
        conciseness=int(payload["conciseness"]),
        format=int(payload["format"]),
        overall=int(payload["overall"]),
    )


async def score_relevance(
    provider: AIProvider,
    preferred_tags: list[str],
    item_tags: list[str],
) -> RelevanceScores:
    prompt = RELEVANCE_RUBRIC.format(tags=preferred_tags, item_tags=item_tags)
    payload = await _ask_judge(provider, prompt, "You are a relevance auditor.")
    return RelevanceScores(
        relevance=int(payload["relevance"]),
        matched=int(payload["matched"]),
        total=int(payload["total"]),
        reason=str(payload.get("reason", "")),
    )


async def score_stability(
    provider: AIProvider,
    stats: dict[str, float | int],
) -> StabilityScores:
    payload = await _ask_judge(provider, STABILITY_RUBRIC.format(stats=stats), "You are an SRE.")
    return StabilityScores(
        stability=int(payload["stability"]),
        bottleneck=str(payload.get("bottleneck", "")),
        reason=str(payload.get("reason", "")),
    )


async def evaluate_sample(
    provider: AIProvider,
    sample_id: str,
    pipeline_state_path: Path,
    preferred_tags: list[str],
) -> EvaluationResult:
    """Read pipeline_state JSON and produce three-dimension scores."""
    state = json.loads(pipeline_state_path.read_text(encoding="utf-8"))
    summary_text = state["summary"]
    selected_items = state.get("selected_items", [])
    item_tags = [tag for item in selected_items for tag in item.get("tags", [])]

    summary = await score_summary(provider, summary_text)
    relevance = await score_relevance(provider, preferred_tags, item_tags)
    stats = {
        "rss_success": state.get("rss_success_rate", 0.0),
        "llm_success": state.get("llm_success_rate", 0.0),
        "publish_success": state.get("publish_success_rate", 0.0),
    }
    stability = await score_stability(provider, stats)

    overall = (summary.overall + relevance.relevance + stability.stability) / 3.0
    return EvaluationResult(
        sample_id=sample_id,
        summary=summary,
        relevance=relevance,
        stability=stability,
        passed=overall >= 7.0,
    )
```

### T4：benchmark.py（批量跑 + 回归检测 + 报告）

```python
# src/multiscribe_agent/eval/benchmark.py
"""Run evaluations across a dataset, detect regressions, emit Markdown reports."""

from __future__ import annotations

import json
from dataclasses import dataclass
from datetime import UTC, datetime
from pathlib import Path

from multiscribe_agent.eval.dataset import Dataset
from multiscribe_agent.eval.evaluator import (
    EvaluationResult,
    JudgeError,
    evaluate_sample,
)
from multiscribe_agent.llm.provider import AIProvider


class RegressionDetected(RuntimeError):
    """Raised when overall score drops more than the regression threshold."""

    def __init__(self, baseline: float, current: float, threshold: float) -> None:
        super().__init__(
            f"Regression: {baseline:.2f} → {current:.2f} "
            f"(drop {baseline - current:.2f} > {threshold:.2f})"
        )
        self.baseline = baseline
        self.current = current
        self.threshold = threshold


@dataclass(frozen=True, slots=True)
class BenchmarkSummary:
    dataset_name: str
    total: int
    passed: int
    failed: int
    avg_summary: float
    avg_relevance: float
    avg_stability: float
    overall: float


async def run_benchmark(
    provider: AIProvider,
    dataset: Dataset,
    preferred_tags: list[str],
    reports_dir: Path,
    baseline_path: Path | None = None,
    regression_threshold: float = 0.10,
) -> BenchmarkSummary:
    """Evaluate all samples and write a Markdown report."""
    reports_dir.mkdir(parents=True, exist_ok=True)
    results: list[EvaluationResult] = []
    for sample in dataset.samples:
        try:
            result = await evaluate_sample(
                provider,
                sample.id,
                Path(sample.input_path),
                preferred_tags,
            )
        except JudgeError as exc:
            raise JudgeError(f"Sample {sample.id} failed: {exc}") from exc
        results.append(result)

    summary = _summarize(dataset, results)
    _write_report(dataset, results, summary, reports_dir)

    if baseline_path is not None and baseline_path.exists():
        baseline = BenchmarkSummary(**json.loads(baseline_path.read_text(encoding="utf-8")))
        if (baseline.overall - summary.overall) > regression_threshold:
            raise RegressionDetected(baseline.overall, summary.overall, regression_threshold)

    baseline_path.parent.mkdir(parents=True, exist_ok=True)
    baseline_path.write_text(
        json.dumps(
            {
                "dataset_name": summary.dataset_name,
                "total": summary.total,
                "passed": summary.passed,
                "failed": summary.failed,
                "avg_summary": summary.avg_summary,
                "avg_relevance": summary.avg_relevance,
                "avg_stability": summary.avg_stability,
                "overall": summary.overall,
            },
            ensure_ascii=False,
            indent=2,
        ),
        encoding="utf-8",
    )
    return summary


def _summarize(dataset: Dataset, results: list[EvaluationResult]) -> BenchmarkSummary:
    total = len(results)
    passed = sum(1 for r in results if r.passed)
    avg_summary = sum(r.summary.overall for r in results) / total
    avg_relevance = sum(r.relevance.relevance for r in results) / total
    avg_stability = sum(r.stability.stability for r in results) / total
    overall = (avg_summary + avg_relevance + avg_stability) / 3
    return BenchmarkSummary(
        dataset_name=dataset.name,
        total=total,
        passed=passed,
        failed=total - passed,
        avg_summary=avg_summary,
        avg_relevance=avg_relevance,
        avg_stability=avg_stability,
        overall=overall,
    )


def _write_report(
    dataset: Dataset,
    results: list[EvaluationResult],
    summary: BenchmarkSummary,
    reports_dir: Path,
) -> Path:
    timestamp = datetime.now(UTC).strftime("%Y%m%d-%H%M%S")
    report_path = reports_dir / f"{dataset.name}_{timestamp}.md"
    lines = [
        f"# 评估报告 — {dataset.name}",
        "",
        f"- 生成时间：{timestamp}",
        f"- 样本数：{summary.total}（通过 {summary.passed}，失败 {summary.failed}）",
        f"- 平均摘要质量：{summary.avg_summary:.2f} / 10",
        f"- 平均相关性：{summary.avg_relevance:.2f} / 10",
        f"- 平均稳定性：{summary.avg_stability:.2f} / 10",
        f"- **总体得分：{summary.overall:.2f} / 10**",
        "",
        "## 样本明细",
        "",
        "| ID | 摘要 | 相关 | 稳定 | 状态 |",
        "|----|------|------|------|------|",
    ]
    for result in results:
        status = "✅" if result.passed else "❌"
        lines.append(
            f"| {result.sample_id} | {result.summary.overall} | "
            f"{result.relevance.relevance} | {result.stability.stability} | {status} |"
        )
    report_path.write_text("\n".join(lines), encoding="utf-8")
    return report_path
```

### T5：CLI 子命令集成

```python
# src/multiscribe_agent/cli.py  追加子命令（保留原有 main 入口不动）

import asyncio
from pathlib import Path

import click

from multiscribe_agent.bootstrap import ServiceContext
from multiscribe_agent.eval.benchmark import run_benchmark
from multiscribe_agent.eval.dataset import load_dataset


@click.command()
@click.option("--dataset", required=True, help="数据集名（不含扩展名，位于 data/eval/datasets/）")
@click.option(
    "--datasets-dir",
    default="data/eval/datasets",
    show_default=True,
    type=click.Path(exists=True, file_okay=False),
)
@click.option("--reports-dir", default="data/eval/reports", show_default=True)
@click.option("--baseline", default="data/eval/baselines/last.json")
@click.option("--regression-threshold", default=0.10, show_default=True, type=float)
def eval(dataset: str, datasets_dir: str, reports_dir: str, baseline: str, regression_threshold: float) -> None:
    """运行 LLM-as-Judge 评估并生成 Markdown 报告。"""
    from multiscribe_agent.cli import _resolve_provider  # reuse existing helper

    context = ServiceContext.bootstrap()
    provider = _resolve_provider(context)
    dataset_path = Path(datasets_dir) / f"{dataset}.yaml"
    loaded = load_dataset(dataset_path)
    summary = asyncio.run(
        run_benchmark(
            provider,
            loaded,
            preferred_tags=context.memory_service.get_preferences().preferred_tags
            if context.memory_service
            else [],
            reports_dir=Path(reports_dir),
            baseline_path=Path(baseline) if baseline else None,
            regression_threshold=regression_threshold,
        )
    )
    click.echo(f"OK {summary.dataset_name} overall={summary.overall:.2f}")
```

### T6：预置数据集

```yaml
# data/eval/datasets/tech_weekly.yaml
name: tech-weekly
description: 技术周报样本（每日推送主流程）
rubric: tech-weekly
samples:
  - id: tw-001
    input_path: tests/eval/fixtures/tw_001.json
    expected_tags: [AI, LLM, agent]
    expected_min_score: 0.7
  - id: tw-002
    input_path: tests/eval/fixtures/tw_002.json
    expected_tags: [python, performance]
    expected_min_score: 0.65
  - id: tw-003
    input_path: tests/eval/fixtures/tw_003.json
    expected_tags: [frontend, design]
    expected_min_score: 0.7
  - id: tw-004
    input_path: tests/eval/fixtures/tw_004.json
    expected_tags: [devops, observability]
    expected_min_score: 0.7
  - id: tw-005
    input_path: tests/eval/fixtures/tw_005.json
    expected_tags: [security, llm]
    expected_min_score: 0.7
```

```yaml
# data/eval/datasets/summary_quality.yaml
name: summary-quality
description: 摘要质量评估（不依赖 P17 记忆）
rubric: summary-quality
samples:
  - id: sq-001
    input_path: tests/eval/fixtures/sq_001.json
    expected_min_score: 0.8
  - id: sq-002
    input_path: tests/eval/fixtures/sq_002.json
    expected_min_score: 0.75
  - id: sq-003
    input_path: tests/eval/fixtures/sq_003.json
    expected_min_score: 0.7
```

### T7：测试覆盖

- `test_dataset.py`：合法 YAML 加载、缺字段报 `ValueError`、空 samples 报 `ValueError`。
- `test_evaluator.py`：mock AIProvider 返回合法 JSON → 三个 score 类构造正确；返回非 JSON 抛 `JudgeError`。
- `test_benchmark.py`：3 个 fixture 跑完产出 `BenchmarkSummary`；baseline 文件触发 `RegressionDetected`；不存在的 baseline 不抛异常；报告文件包含表格。
- `fixtures/*.json`：最小化 pipeline_state JSON（summary / selected_items / 三个成功率）。

---

## 六、测试与质量门

```bash
.venv\Scripts\python.exe -m pytest tests/eval -v -p no:cacheprovider --basetemp .pytest-tmp-p21

# 全量
.venv\Scripts\python.exe -m pytest -q -p no:cacheprovider

# 静态门
.venv\Scripts\python.exe -m ruff check .
.venv\Scripts\python.exe -m ruff format --check .
.venv\Scripts\python.exe -m mypy src
```

**手动冒烟**（可选，依赖 LLM 凭据）：
```bash
.venv\Scripts\python.exe -m multiscribe_agent eval --dataset summary-quality
# 期望：输出 OK summary-quality overall=8.xx
```

---

## 七、完成定义

- [ ] 白名单 14 个文件全部创建/修改
- [ ] `pytest tests/eval` 全绿（≥ 12 用例）
- [ ] `pytest -q` 全量无回归
- [ ] ruff / mypy 全绿
- [ ] CLI `multiscribe-agent eval --help` 可执行
- [ ] 至少 1 个数据集可加载并产出 Markdown 报告（冒烟）
- [ ] `codex/reviews/P21-REVIEW.md` 填写完毕

---

## 八、文件清单

```
src/multiscribe_agent/eval/__init__.py            [新增]
src/multiscribe_agent/eval/__main__.py            [新增]
src/multiscribe_agent/eval/dataset.py             [新增]
src/multiscribe_agent/eval/evaluator.py           [新增]
src/multiscribe_agent/eval/benchmark.py           [新增]
src/multiscribe_agent/eval/judge_prompts.py       [新增]
data/eval/datasets/tech_weekly.yaml               [新增]
data/eval/datasets/summary_quality.yaml           [新增]
src/multiscribe_agent/cli.py                      [修改]
tests/eval/__init__.py                            [新增]
tests/eval/test_dataset.py                        [新增]
tests/eval/test_evaluator.py                      [新增]
tests/eval/test_benchmark.py                      [新增]
tests/eval/fixtures/tw_001.json                   [新增]
tests/eval/fixtures/tw_002.json                   [新增]
tests/eval/fixtures/tw_003.json                   [新增]
tests/eval/fixtures/tw_004.json                   [新增]
tests/eval/fixtures/tw_005.json                   [新增]
tests/eval/fixtures/sq_001.json                   [新增]
tests/eval/fixtures/sq_002.json                   [新增]
tests/eval/fixtures/sq_003.json                   [新增]
```