# Codex 完工 Review 模板

> 执行 Agent（Codex）每完成一个 phase 包后，**必须**按本模板填写 review，交决策者转规划判定。
> 填写原则：**只认证据，不认声明**。每条都要给可验证的证据（命令原始输出 / 测试名 / 截图 / 文件路径）。

---

# Review: `<Px-名称>`（如 P0-工程基线）

**执行包**：`docs/phases/Px-*.md`
**完成日期**：YYYY-MM-DD
**执行者**：Codex

## 1. 范围核对

### 1.1 实际改动文件清单（创建/修改）

> 列出所有动过的文件（含新增）。

| 文件路径 | 操作（新增/修改） | 用途 |
| :--- | :--- | :--- |
| `src/...` | 新增 | ... |
| `pyproject.toml` | 修改 | 追加依赖 |

### 1.2 白名单合规性

- [ ] 上述所有文件均在 `Px-*.md`「可改范围」白名单内。
- [ ] 「禁止改动」文件**零触碰**。

> 若有偏离，在此**显式声明**并说明原因（原则上应停下问而非擅改）。

## 2. 验收条件逐条对照

> 对照 `Px-*.md` 的「验收条件」逐条。每条给证据。

| # | 验收条件 | 状态 | 证据 |
| :--- | :--- | :--- | :--- |
| 1 | （复制验收条件原文） | ✅/❌ | （命令输出 / 测试名 / 截图引用 / 文件:行） |
| 2 | ... | ... | ... |

## 3. 测试与质量门（贴原始输出）

> 贴每条命令的**原始输出**（不要只写"通过"）。

### 3.1 `uv run ruff check .`
```
（贴原始输出）
```

### 3.2 `uv run ruff format --check .`
```
（贴原始输出）
```

### 3.3 `uv run mypy src`
```
（贴原始输出）
```

### 3.4 `uv run pytest -q`
```
（贴原始输出，含测试数 / 通过数 / 耗时）
```

### 3.5 e2e（如该包要求，如 P7/P8/P13）
```
（贴 uv run pytest -m e2e -q 输出）
```
附截图：`![飞书推送](path/to/screenshot)`（手动测试证据）

## 4. 详细任务完成情况

> 对照 `Px-*.md`「详细任务」T1/T2/...，简述每项的实现要点（2-3 句 + 关键文件:行）。

- **T1 ...**：实现了 ...，见 `src/xxx.py:NN`。
- **T2 ...**：...

## 5. 规范符合性自检

- [ ] 全量类型注解，无裸 `Any`。
- [ ] 异步 I/O 用 `async def`，无 async 内阻塞 I/O。
- [ ] HTTP 用 `httpx.AsyncClient`，DB 用 `aiosqlite`。
- [ ] 错误处理：try/except 具体异常 + structlog。
- [ ] 日志脱敏（无明文 token/secret/webhook）。
- [ ] 分层依赖方向正确（domain 零外部依赖，无反向依赖）。
- [ ] 测试无真实网络调用（e2e 已标记）。

## 6. 新增依赖

> 列出本包对 `pyproject.toml` 新增的依赖（若有）。

| 包 | 版本约束 | 用途 |
| :--- | :--- | :--- |

## 7. 风险、遗留与取舍

> 诚实标注。规划 review 会重点看这里。

- **风险**：（如"Google Provider 未实现，用 NotImplementedError 占位，记入 P18 补"）
- **遗留**：（如"xxx 边界场景未覆盖，建议 P?? 补测试"）
- **取舍**：（如"FTS5 中文分词用了 unicode61，对中文召回一般，P16 混合检索会缓解"）
- **未做的事**：（明确列出任务包没要求、你也没做的，避免误解）

## 8. BLOCKED 项（若有）

> 若有停下问的情况，在此说明。没有则写"无"。

- **阻塞点**：（如"P1 的 PluginMetadata 未在 domain/models.py 定义，本包无法继续，需先在 P1 补充"）
- **已等待**：决策者/规划指示。

## 9. 对后续包的提示

> 你的实现可能影响后续包，在此提示（如接口形状、约定）。

- （如"DigestItem 定义在 renderers/models.py，P8 企微应 import 复用"）

## 10. 自评

- 我认为本包**满足** `Px-*.md` 的完成定义：✅/❌
- 若 ❌，原因：...

---

> **填完后**：交决策者。决策者把本 review 发回规划（ZCode），由规划按六条标准（范围合规/验收有据/测试全绿/规范干净/无回归/风险诚实）判定放行或退回。
