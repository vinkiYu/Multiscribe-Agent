# Review: `P0.7-API中转端点支持`

**执行包：** `docs/phases/P0.7-API中转端点支持.md`

**完成日期：** 2026-07-18

**执行者：** Codex

## 1. 范围核对

| 文件路径 | 操作 | 用途 |
| :--- | :--- | :--- |
| `src/multiscribe_agent/config.py` | 修改 | OpenAI/Anthropic Base URL Settings 别名与 ProviderConfig 绑定 |
| `.env.example` | 修改 | 中转端点占位和说明 |
| `tests/test_base_url_binding.py` | 新增 | Base URL 绑定、空值保留、与 API key 独立性测试 |
| `codex/reviews/P0.7-REVIEW.md` | 新增 | 按全局执行规范记录验收证据 |

所有应用与测试改动均在 P0.7 白名单内。未修改 `ProviderConfig.base_url` 的字段结构、`llm/providers/` 或 `bootstrap.py`。`DESIGN.md` 和 `docs/BRAND.md` 是用户已有未跟踪文件，未改动。

## 2. 验收条件

| # | 条件 | 状态 | 证据 |
| :--- | :--- | :--- |
| 1 | OpenAI provider 的 `base_url` 等于非空 `OPENAI_API_BASE_URL` | 通过 | `SystemSettings.openai_api_base_url` 使用 `AliasChoices`，validator 设置 `ProviderConfig.base_url`；`test_openai_base_url_binds_to_default_provider`。 |
| 2 | Anthropic provider 的 `base_url` 等于非空 `ANTHROPIC_API_BASE_URL` | 通过 | 对应 Anthropic 字段和 validator 分支；`test_anthropic_base_url_binds_to_default_provider`。 |
| 3 | 空 OpenAI Base URL 不覆盖已有值 | 通过 | `test_empty_base_url_keeps_explicit_provider_value`。 |
| 4 | 全测试绿，不含真实网络 | 通过 | 下列质量门和全量 pytest 输出；新增测试不执行 HTTP 调用。 |

## 3. 测试与质量门（原始输出）

### `python -m pytest tests/test_base_url_binding.py -q -p no:cacheprovider`

```text
....                                                                     [100%]
4 passed in 0.07s
```

### `python -m ruff check .`

```text
All checks passed!
```

### `python -m ruff format --check .`

```text
117 files already formatted
```

### `python -m mypy src`

```text
Success: no issues found in 74 source files
```

### `python -m pytest -q -p no:cacheprovider`

```text
130 passed, 4 deselected in 7.07s
```

## 4. 实现说明

- `OPENAI_API_BASE_URL` / `MULTISCRIBE_OPENAI_API_BASE_URL` 和 Anthropic 对应变量均可进入 Settings；Base URL 与 API key 是独立字段。
- 既有 Provider 构造已将 `ProviderConfig.base_url` 传给 LangChain，因此本包没有修改 Provider 或 bootstrap。
- `.env.example` 说明中转端点是可选项，不记录任何真实中转地址或密钥。

## 5. 风险与后续

- 当前本机 `.env` 未配置 `OPENAI_API_BASE_URL` 或 `ANTHROPIC_API_BASE_URL`，因此本包未重跑真实 P13 e2e。填写中转端点、兼容模型名和 API key 后，运行 `uv run pytest tests/e2e -m e2e -q` 验证真实 RSS、LLM 和飞书投递。
- P13 的 Docker Desktop daemon 与飞书/企微截图验收仍需在运行环境恢复后完成，P0.7 不改变该状态。

## 6. 自评

本包满足 `P0.7-API中转端点支持.md` 的完成定义：**是**。
